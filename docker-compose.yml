services:
  keycloak:
    image: quay.io/keycloak/keycloak:latest
    # Set admin credentials via environment variables. 
    # Create a .env file with KEYCLOAK_ADMIN_USERNAME and KEYCLOAK_ADMIN_PASSWORD.
    # Never use default credentials in non-local environments.
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KEYCLOAK_ADMIN_USERNAME}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD}
    ports:
      - "8088:8080"
    command:
      - start-dev

  # app:
  #   build: .
  #   ports:
  #     - "8082:8082"
  #   depends_on:
  #     - keycloak